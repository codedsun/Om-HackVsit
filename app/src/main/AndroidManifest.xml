<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.suneetsrivastava.om">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MainActivity" />
        <activity android:name=".Activities.SplashScreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Activities.Help2sleepmenu" />
        <activity android:name=".Activities.VisualSleep" />
        <activity android:name=".ShortStories">
        </activity>
        <activity android:name=".SleepStory" />
        <activity android:name=".Activities.SleepMusic">

        </activity>

        <activity android:name=".Activities.LandingActivity" />
        <activity android:name=".Activities.MeditationActivity" />
        <activity android:name=".Activities.MeditationDetails"></activity>

    </application>

</manifest>